<%= form_for(nothing) do |f| %>
  <% if nothing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nothing.errors.count, "error") %> prohibited this nothing from being saved:</h2>

      <ul>
      <% nothing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
