<p id="notice"><%= notice %></p>

<p>
  <strong>Size</strong>
  <%= @room.size %>
</p>

<p>
  <strong>Number:</strong>
  <%= @room.number %>

</p>

<p>
  <strong>Vacant:</strong>
  <%= @room.vacant %>
</p>

<p>
  <strong>Building:</strong>
  <%= @room.building %>
</p>

<% if current_user.privilege != 'Member' %>

<p>
  <strong>Booking History:</strong>
</p>

<table style="border: dotted;margin-left: 20px">
  <thead>
  <tr>
    <th>Starts at</th>
    <th>By Whom</th>
    <th colspan="2"></th>
  </tr>
  </thead>

  <tbody>
  <% @room.appoints.each do |appoint| %>
      <tr>
        <td><%= appoint.start_time %></td>
        <td><%= User.find(appoint.user_id).name %></td>

      </tr>
  <% end %>
  </tbody>
</table>

<% end %>


<p></p>
<p></p>
<%= link_to 'Edit', edit_room_path(@room) %> |
<%= link_to 'Back', rooms_path %>
<%= link_to 'Appo', new_appoint_path %>
<%= link_to 'Show Appo',appoints_path(room_id: @room.id) %>


